# Daily Notebook — 2026-03-08

## 11:30 — Physics Loss Audit: Critical Issues Discovered

**Files affected:** `src/physics_loss.py` (NavierStokesPhysicsLoss, lines 114-1117)

**Problem:** 5-team research audit revealed the active physics loss has fundamental correctness and training stability issues.

**Root causes (3 critical):**
1. **Divergence normal direction WRONG** (L168-169): Uses edge tangent `(dx/r, dy/r)` instead of perpendicular face normal `(dy/r, -dx/r)`. Continuity loss computes something that is not ∇·u.
2. **Loss scale mismatch**: Momentum residual O(10,000) vs data loss O(0.01-1.0). No non-dimensionalization by reference acceleration `U²/L`. Curriculum ramp causes catastrophic forgetting.
3. **Two-pass Laplacian** (L267-289): `grad(grad(f))` = 18 scatter_add + double-smoothing → viscous term systematically underestimated.

**Additional issues:** Missing ∇u^T in viscous stress (L712-713), duplicate `weighted_gradient(uv)` call (L692-697, wastes 6 scatter_add), single global RBF bandwidth, Huber delta miscalibrated.

**Fix:** Not yet implemented. Full audit saved to `docs/plans/2026-03-08-physics-loss-audit.md`. Irony: the unused `UnifiedNavierStokesPhysicsLoss` class has the correct divergence formula.

**Impact:** Physics loss is currently providing weak/incorrect training signal. Fixing P0 issues should substantially improve physics-informed training effectiveness.

## HPO Mid-Run Snapshot — 2026-03-08 (2.50h elapsed)

**Status:** running | **Phase:** tpe

**Progress:** Trial 54/60 | 32 complete | 21 pruned

**Best val loss:** 0.076073

**Best params so far:**
  - `hidden_dim`: 48
  - `num_layers`: 10
  - `dropout`: 0.0
  - `num_global_tokens`: 3
  - `lr`: 0.0010560685583058836
  - `weight_decay`: 3.868847182180628e-05
  - `optimizer_type`: adamw
  - `scheduler_type`: ReduceLROnPlateau
  - `scheduler_T_max`: 190
  - `scheduler_gamma`: 0.76
  - `cont_weight`: 2.568749299641597e-05
  - `mom_weight`: 4.068716384173128e-06
  - `bc_weight`: 5.6948127513085544e-05
  - `cont_target`: 0.00027432299436490885
  - `mom_target`: 1.811095341773555e-05
  - `ramp_start_epoch`: 35
  - `ramp_epochs`: 10
  - `batch_size`: 2
  - `grad_clip`: 7.5
  - `amp`: False
  - `label_smoothing`: 0.01

---
